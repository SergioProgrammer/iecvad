---
import Layout from '../layouts/Layout.astro';

const transferencias = [
	{
		titulo: 'Guía para equipos directivos',
		descripcion:
			'Protocolos de respuesta ante emergencias sanitarias, planes de apoyo personalizado y recomendaciones de accesibilidad universal.',
		formato: 'Manual interactivo, fichas descargables y sesiones formativas.'
	},
	{
		titulo: 'Observatorio de inclusión post-pandemia',
		descripcion:
			'Panel con indicadores de seguimiento, relatos de buenas prácticas y recursos abiertos para docentes y familias.',
		formato: 'Plataforma digital con boletines semestrales y alertas temáticas.'
	},
	{
		titulo: 'Laboratorios de co-diseño',
		descripcion:
			'Espacios colaborativos donde comunidades educativas prototipan apoyos personalizados, metodologías flexibles y tecnologías accesibles.',
		formato: 'Talleres presenciales/virtuales y repositorio de prototipos.'
	}
];

const hitos = [
	{ periodo: '2023-2024', detalle: 'Pilotaje de instrumentos y publicación del primer informe insular.' },
	{ periodo: '2024-2025', detalle: 'Lanzamiento del observatorio y despliegue de laboratorios territoriales.' },
	{ periodo: '2025', detalle: 'Entrega de recomendaciones normativas y cierre del plan de acompañamiento.' }
];

const pageTitle = 'Transferencia | Inclusión educativa y calidad de vida';
const pageDescription =
	'Plan de transferencia del proyecto sobre inclusión educativa y calidad de vida del alumnado con discapacidad en Canarias.';
---

<Layout title={pageTitle} description={pageDescription}>
	<div class="relative isolate px-6 pb-20 pt-12">
		<div
			class="pointer-events-none absolute inset-0 -z-10 opacity-70 blur-3xl"
			aria-hidden="true"
			style="background: radial-gradient(circle at 10% 20%, rgba(189,229,255,0.9), transparent 50%), radial-gradient(circle at 80% 10%, rgba(211,235,255,0.6), transparent 40%);"
		></div>

		<article class="mx-auto max-w-5xl space-y-16">
			<header class="space-y-6 text-center">
				<p class="text-xs font-semibold uppercase tracking-[0.4em] text-sky-700">Transferencia</p>
				<h1 class="text-4xl font-semibold text-stone-900">De la evidencia a la acción</h1>
				<p class="mx-auto max-w-3xl text-base text-stone-700">
					La investigación se traduce en productos útiles para centros, administraciones y familias. Cada pieza
					incluye instrucciones de uso, acompañamiento técnico y mecanismos de evaluación de impacto.
				</p>
			</header>

			<section class="grid gap-6 md:grid-cols-3">
				{transferencias.map(({ titulo, descripcion, formato }) => (
					<article class="rounded-3xl border border-sky-100 bg-white/80 p-6 shadow-md shadow-sky-100/60">
						<h2 class="text-xl font-semibold text-stone-900">{titulo}</h2>
						<p class="mt-3 text-sm text-stone-700">{descripcion}</p>
						<div class="mt-4 rounded-2xl border border-sky-100 bg-sky-50/70 p-4 text-sm text-stone-800">
							<strong class="text-xs font-semibold uppercase tracking-[0.3em] text-sky-700"
								>Formato</strong
							>
							<p class="mt-2">{formato}</p>
						</div>
					</article>
				))}
			</section>

			<section class="rounded-[32px] border border-sky-100 bg-gradient-to-br from-white via-sky-50 to-blue-50/80 p-8 shadow-xl shadow-sky-100/60">
				<h2 class="text-2xl font-semibold text-stone-900">Hitos y acompañamiento</h2>
				<p class="mt-3 text-stone-700">
					Cada producto se lanza con sesiones demostrativas, materiales de autoformación y seguimiento remoto.
					Los hitos a continuación sirven como guía para centros y entidades que deseen sumarse al itinerario.
				</p>
				<div class="mt-8 space-y-4">
					{hitos.map(({ periodo, detalle }) => (
						<div class="rounded-3xl border border-white/60 bg-white/80 p-5">
							<p class="text-xs font-semibold uppercase tracking-[0.35em] text-sky-700">{periodo}</p>
							<p class="mt-2 text-sm text-stone-800">{detalle}</p>
						</div>
					))}
				</div>
			</section>
		</article>
	</div>
</Layout>
