---
import Layout from '../layouts/Layout.astro';

const objetivos = [
	{
		titulo: 'Objetivo 1 · Inclusión',
		descripcion:
			'Analizar las prácticas inclusivas y la respuesta educativa desarrollada por los centros desde el comienzo de la pandemia, con especial atención al profesorado NEAE y docentes implicados en la escolarización de alumnado con discapacidad.',
		resultado: 'Documentar estrategias inclusivas y necesidades detectadas para orientar futuras actuaciones.'
	},
	{
		titulo: 'Objetivo 2 · Calidad de vida',
		descripcion:
			'Conocer la autopercepción de la calidad de vida del alumnado con discapacidad durante y después de la pandemia en Canarias.',
		resultado: 'Generar indicadores que permitan evaluar y mejorar el bienestar integral del alumnado.'
	}
];

const fases = [
	{
		nombre: 'Observación',
		items: ['Revisión documental', 'Encuestas a centros', 'Indicadores académicos y sociales']
	},
	{
		nombre: 'Contrastación',
		items: ['Entrevistas en profundidad', 'Laboratorios de co-diseño', 'Validación con expertos']
	},
	{
		nombre: 'Acción',
		items: ['Acompañamiento técnico', 'Publicaciones y guías', 'Seguimiento de implementación']
	}
];

const pageTitle = 'Objetivos | Inclusión educativa y calidad de vida';
const pageDescription =
	'Objetivos principales del proyecto sobre inclusión educativa y calidad de vida del alumnado con discapacidad en Canarias.';
---

<Layout title={pageTitle} description={pageDescription}>
	<div class="relative isolate safe-px pb-20 pt-12 sm:px-6">
		<div
			class="pointer-events-none absolute inset-0 -z-10 opacity-70 blur-3xl"
			aria-hidden="true"
			style="background: radial-gradient(circle at 20% 25%, rgba(186,227,255,0.8), transparent 45%), radial-gradient(circle at 70% 10%, rgba(206,232,255,0.6), transparent 40%);"
		></div>

		<article class="mx-auto max-w-5xl space-y-16">
			<header class="rounded-[32px] border border-sky-100 bg-white/85 p-6 shadow-lg shadow-sky-100/60 sm:p-8">
				<div class="flex flex-col gap-4 text-center md:flex-row md:items-stretch md:gap-6 md:text-left">
					<div class="overflow-hidden rounded-3xl border border-sky-100 md:w-3/5">
						<img
							src="/banner-1.webp"
							alt="Banner Objetivos"
							class="h-56 w-full object-cover sm:h-72 md:h-full"
							loading="lazy"
						/>
					</div>
					<div class="space-y-4 md:flex md:w-2/5 md:flex-col md:justify-center">
						<p class="text-xs font-semibold uppercase tracking-[0.4em] text-sky-700 text-center md:text-left">
							Objetivos
						</p>
						<h1 class="text-balance text-4xl font-semibold text-stone-900">Preguntas que guían la investigación</h1>
						<p class="text-pretty mx-auto max-w-3xl text-base leading-relaxed text-stone-700 md:mx-0">
							Los objetivos trazan una hoja de ruta que une diagnóstico, medición de la calidad de vida,
							identificación de innovaciones y transferencia efectiva. Cada meta se concreta en resultados
							observables y productos que acompañarán a la comunidad educativa.
						</p>
					</div>
				</div>
			</header>

			<section class="grid gap-6 md:grid-cols-2">
				{objetivos.map(({ titulo, descripcion, resultado }, idx) => (
					<article class="rounded-3xl border border-sky-100 bg-white/80 p-6 shadow-md shadow-sky-100/60">
						<p class="text-xs font-semibold uppercase tracking-[0.35em] text-sky-700">Objetivo</p>
						<h2 class="text-balance mt-2 text-2xl font-semibold text-stone-900">{titulo}</h2>

						<p class="text-pretty break-anywhere mt-3 text-sm leading-relaxed text-stone-700">{descripcion}</p>
						<div class="mt-4 rounded-2xl border border-sky-100 bg-sky-50/70 p-4">
							<p class="text-xs font-semibold uppercase tracking-[0.25em] text-sky-700">
								Resultado esperado
							</p>
							<p class="text-pretty break-anywhere mt-1 text-sm leading-relaxed text-stone-800">{resultado}</p>
						</div>
					</article>
				))}
			</section>

			<section class="rounded-[32px] border border-sky-100 bg-gradient-to-br from-white via-sky-50 to-blue-50/80 p-8 shadow-xl shadow-sky-100/50">
				<h2 class="text-balance text-2xl font-semibold text-stone-900">Fases operativas</h2>
				<p class="text-pretty mt-3 leading-relaxed text-stone-700">
					El avance del proyecto se estructura en tres momentos conectados. Cada fase produce entregables que
					se comparten con la comunidad educativa para recibir retroalimentación y ajustar las acciones de
					acuerdo con las necesidades reales.
				</p>
				<div class="mt-8 grid gap-6 md:grid-cols-3">
					{fases.map(({ nombre, items }) => (
						<article class="rounded-3xl border border-white/60 bg-white/70 p-5">
							<p class="text-xs font-semibold uppercase tracking-[0.35em] text-sky-700">Fase</p>
							<h3 class="text-balance mt-2 text-xl font-semibold text-stone-900">{nombre}</h3>
							<ul class="mt-3 space-y-2 text-sm leading-relaxed text-stone-700">
								{items.map((item) => (
									<li class="break-anywhere">• {item}</li>
								))}
							</ul>
						</article>
					))}
				</div>
			</section>
		</article>
	</div>
</Layout>
